<%- contentFor('hero') %>
<section class="hero">
  <div class="hero-content">
    <div class="center-block">
    <img src="/img/headers/news.jpg" class="img-responsive" alt="Responsive image">
  </div>
  </div>
</section>

<%- contentFor('body') %>

<div class="about-photos"

  <div class="row">
    <% for (user of users) { %>
      <div class="col-sm-4">
          <img src="<%= user.avatar %>" alt="" class="img-responsive">
      </div>
    <% } %>
  </div>
</div>

<div class="about-photos"

  <div class="row">
    <% for (user of users) { %>
      <div class="col-sm-4">
          <img src="<%= user.avatar %>" alt="" class="img-responsive">
      </div>
    <% } %>
  </div>
</div>
